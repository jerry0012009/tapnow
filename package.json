{
  "name": "tapnow-canvas-audit",
  "version": "0.1.11",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit-canvas.mjs",
    "export": "node scripts/export-canvas.mjs",
    "backup:real-canvas": "node scripts/backup-real-canvas.mjs",
    "viewer": "node scripts/serve-backup-viewer.mjs",
    "record": "node scripts/record-session.mjs",
    "extension:dev": "wxt --browser chrome",
    "extension:build": "wxt build --browser chrome --mv3",
    "extension:zip": "wxt zip --browser chrome --mv3",
    "test": "tsx --test"
  },
  "dependencies": {
    "playwright": "1.62.1"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "wxt": "0.21.4"
  }
}
